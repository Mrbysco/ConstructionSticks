* Fix `Open Stick Options` keybind behavior
* Add `Toggle Random` keybind to toggle random stick mode on/off
* Change config type to `STARTUP` instead of `SERVER`, so that mods that access items early won't crash

## This update changes the config type
**This means that the config will now be called `constructionstick-startup.toml`, make sure to rename your existing config if you've made any changes**